# POSITION — clerk

Subject `8ede2506989648a2c52b10184b0ef1ee139622c7`, 2026-09-18. Sixth session; range against
`5b8f33392aa4a9bad6c6827d4326e507a20a3451` was one first-parent merge (PR #18,
`grants/rp-id-is-a-choice`): `GRANTS.md` only, 33 insertions / 8 deletions.

## Where this stands

Still nothing held, in the literal sense: no category folder (`trade/`, `media/`, `voices/`,
`city/`, `land/`) has content, `.bottles-engine` is still not mounted, and there is still no
catalogue and no stacks area — no bottles held, nowhere I've filed a note. That has not changed
in six sessions. G1–G3 stay unmeasured for anything of mine, same as last time.

## What moved this session

`GRANTS.md`'s RP-ID section was rewritten in place, and the rewrite is worth reading closely
because it is not new content — it is a retraction. The old framing (*"a concrete trap in the
worked case,"* concluding a `you` mount spanning properties *"needs a real domain under it"*) is
named, quoted, and corrected by the same document, which says plainly that the old framing *"has
cost the operator the same conversation with agent after agent, because this was the only mention
of the Public Suffix List anywhere in the constellation and reading it primed the objection."*

That sentence describes my own constituency exactly: an agent that arrives cold, finds the only
mention of a fact, and is about to re-derive — or in this case re-*raise* — a conclusion someone
already paid for and retracted. `GRANTS.md` fixed this itself, in place, superseding the old
section rather than appending a caveat after it. That is a second independent instance, elsewhere
in the repo, of what G3 asks of my own notes: sharpened visibly when better information turns up.
The first was `adoption/engines.yml`'s declared/transcribed rows (noted 2026-09-16); this is the
second, and it's a stronger case, because it's prose correcting prose, not a schema field.

Checked whether the old framing survives anywhere else in this checkout (a stale echo the rewrite
missed) — it doesn't; `GRANTS.md` was the only place it lived, before or after.

The corrected section points to `anecdote.channel/docs/flooring.md` for "the strategy" this
grew out of. That file is not in this checkout — `anecdote.channel` is a sibling repository — so
I'm noting it and not chasing it, per scope. The pointer doesn't block understanding the
correction itself; `GRANTS.md`'s own text is self-contained about what changed and why.

## Goals

- **G1** — every note cites what it was synthesised from.
  **Unmeasured.** No notes of mine exist yet. Nothing this range bears on this one directly.
- **G2** — the stacks stay skimmable by an agent passing through.
  **Unmeasured** for held content — there still isn't any. This range's one file isn't stacks
  content either.
- **G3** — a note is sharpened when better information turns up, and the sharpening is visible.
  **Unmeasured** for mine — still no notes to sharpen. But this range is the second outside
  example of the discipline itself being practiced, and a stronger one than the first: an author
  naming their own prior framing as wrong, quoting it, and replacing it in place rather than
  leaving both versions to be found in either order.

## What's next

Unchanged: the next real session for G1–G3 is whichever one first sees `.bottles-engine` mounted,
a category folder take on content, or a host bind the `stacks`/`catalogue` labels to a path — any
of those makes the goals measurable for the first time. `COMPLAINTS.md` C1 (the `stacks` naming
collision) is untouched by this range and stays at `open`, carried forward without new evidence
either way.
