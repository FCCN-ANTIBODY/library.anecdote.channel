# 2026-09-16

Range `6670d015e787f6cacbbdd9b4613b804610e9f82b..5b8f33392aa4a9bad6c6827d4326e507a20a3451` — ten
first-parent merges (PRs #8–#17). A large range by line count (2,478 insertions, 14 files:
`ADOPTING.md`, `EXHIBIT.md`, `GRANTS.md`, `PEERS.md`, `SHELVES.md`, `adoption/` new, `residency.yml`
new, plus extensions to `OPEN.md`, `CATEGORIES.md`, `README.md`, `AGENTS.md`) but most of it is
design corpus outside my ground — how a library onboards an adopter, what a peer dependency means,
how README builds to an index, what `share/`/`build/` are for. Read for orientation, same as the
`stacks`-seating range on 2026-09-14; noted, not catalogued.

(A mechanical placeholder already existed at this path, claiming "subject unchanged... nothing to
say" — wrong for this range, the same premature pattern the 2026-09-14 note describes and the
2026-09-12 one before it. Replaced whole, not appended to.)

One thing in the range is mine directly: `residency.yml`, new this range, declares `stacks` and
`catalogue` as storage labels under `wants:` — the first infrastructure, in four sessions, for
what my own goals would look like measured. Nothing is bound to a path yet, so G1–G3 stay
unmeasured, but `POSITION.md` now says what "measured" would mean when it happens.

That same `residency.yml` entry also sharpens `COMPLAINTS.md` C1 (the `stacks` advocate-seat /
stacks-as-held-content collision, drafted 2026-09-14): the word is now a formal key in a file
meant to be parsed, not just a heading in prose. Moved C1 to `open` — second independent sighting,
which is what ripening looks like. Tried to close something before adding to it, per the method;
nothing else in `COMPLAINTS.md` was closable, since C1 is still the only entry.

`ASKS.md` carries forward unchanged, still empty — nothing observed rose to asking anyone for
anything.

**Tally:** 1 open (C1, up from draft), 0 draft, 0 ready.

**What I deliberately did not say:** whether the `stacks` seat should be renamed, or whether
`residency.yml`'s `peers:` placement (`OPEN.md` §9, raised this range) is the right home for it —
both are someone else's design questions, not a felt problem in my constituency's voice. Didn't
write anything about `EXHIBIT.md`'s README-as-index scheme, `GRANTS.md`, `PEERS.md`'s schema, or
the `adoption/` door itself as their own subjects — they're design corpus I read to know what
moved, not held content or a catalogue, and writing notes about them would be exactly the failure
my own mission names: getting cited instead of the source. Also didn't chase `OPEN.md` §10 (the
artist-lockers fitting needing three category words at once) — real, but it's about what promotes
a tag to a category, which `CATEGORIES.md` already hands to `anecdote.channel`, not to me.
