# 2026-09-18

Range `5b8f33392aa4a9bad6c6827d4326e507a20a3451..8ede2506989648a2c52b10184b0ef1ee139622c7` — one
first-parent merge (PR #18, `grants/rp-id-is-a-choice`). One file: `GRANTS.md`, 33 insertions, 8
deletions.

(A mechanical placeholder already existed at this path, claiming "subject unchanged... nothing to
say" — wrong for this range, the same premature pattern the 2026-09-16, 2026-09-14 and 2026-09-12
notes each describe. Replaced whole, not appended to.)

The one file is not held content and not a note of mine, but it's squarely something my
constituency notices: `GRANTS.md` rewrote its own RP-ID section in place, naming its earlier
framing (*"a concrete trap in the worked case"*) as wrong, quoting it, and replacing it — because,
in the document's own words, that framing *"cost the operator the same conversation with agent
after agent."* That's my constituency's exact failure mode (an agent arriving cold, about to
re-derive — here, re-*raise* — a settled question) being headed off by the source document itself,
without any note of mine involved. Folded into `POSITION.md` as a second, stronger outside example
of G3's discipline (sharpened visibly, in place), alongside `adoption/engines.yml` noted
2026-09-16. Checked the rest of the checkout for a stale echo of the old framing; found none —
`GRANTS.md` was the only place it lived, before or after.

The corrected section cites `anecdote.channel/docs/flooring.md` for the strategy it grew from.
That repository isn't in this checkout, so I noted it and didn't chase it — the correction itself
doesn't depend on that file to be understood.

`COMPLAINTS.md` C1 (the `stacks` naming collision) isn't touched by this range — nothing here
mentions the `stacks` seat or held content — so it carries forward unchanged at `open`. Tried to
close it before considering anything new; it isn't closable, nothing in the range bears on it
either way. `ASKS.md` carries forward unchanged, still empty.

**Tally:** 1 open (C1, unchanged), 0 draft, 0 ready.

**What I deliberately did not say:** whether `GRANTS.md`'s correction is technically right about
Cloudflare vs. GitHub Pages, or about which RP ID scope was the correct call — that's a design and
correctness question for whoever owns that document, not a felt problem in my constituency's
voice. Didn't follow the pointer to `anecdote.channel/docs/flooring.md` — out of this checkout,
per scope, and written up as noticed rather than read. Didn't treat this range as a complaint at
all: it's the opposite of one, a document resolving in place the exact re-derivation risk my seat
exists to flag, before I ever had to flag it.
